#!/bin/sh
set -e

test "$1" = 'configure' || exit 0

sh -c 'cd /package/admin/daemontools-0.76 && package/upgrade && package/run' \
  || exit 1
dpkg --compare-versions "$2" ge '0.76-0' || kill -s HUP 1
